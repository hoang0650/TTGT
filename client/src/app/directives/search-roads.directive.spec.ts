import { SearchRoadsDirective } from './search-roads.directive';

describe('SearchRoadsDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchRoadsDirective();
    expect(directive).toBeTruthy();
  });
});
