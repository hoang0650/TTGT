version: '3.5'

services:
  backend:
    image: tgtt-backend
    ports: 
      - 3000:3000
    build:
      context: ../backend
      dockerfile: Dockerfile

  client:
    image: tgtt-client
    ports: 
      - 9000:9000
    build:
      context: ../client
      dockerfile: Dockerfile